# Examples

## Current package/CLI examples

- `minimal.py`: minimal public package API usage (`verify_answer` + `PipelineConfig`).
- `pipeline_demo/`: file inputs used by the `ega pipeline` CLI command.
- `run_pipeline_demo.sh`: convenience wrapper for the same pipeline demo assets.

## Legacy examples

- `v2/`: legacy evaluation/reproduction examples retained for historical comparison.
